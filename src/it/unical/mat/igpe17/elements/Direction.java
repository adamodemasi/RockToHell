package it.unical.mat.igpe17.elements;

import java.io.Serializable;

public enum Direction implements Serializable{
		UP(), LEFT(), RIGHT(), DOWN(), STOPDX(), STOPSX(), UPSX(), UPDX(), FLYDX(), FLYSX();
		
}
